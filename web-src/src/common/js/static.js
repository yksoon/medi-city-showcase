// 사전등록 idx
const registration_idx = "2";

// 썸네일 formData append
const imageResizeOptions = {
    maxSizeMB: 0.25,
    useWebWorker: true,
    preserveExif: true,
};

export { registration_idx, imageResizeOptions };
